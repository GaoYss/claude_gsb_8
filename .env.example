# docker compose 可覆盖的变量（复制为 .env 后生效）

# PostgreSQL 连接信息
POSTGRES_DB=green_space
POSTGRES_USER=green_space
POSTGRES_PASSWORD=green_space_pwd

# 宿主机端口
FRONTEND_PORT=8090
BACKEND_PORT=5000

# 首次启动是否写入演示数据（true / false）
SEED_DEMO_DATA=true

# 允许跨域的来源，容器内由 nginx 同源代理，默认 * 即可
CORS_ORIGINS=*
